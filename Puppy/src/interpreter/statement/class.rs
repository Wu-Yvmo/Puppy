use crate::{ast::Class, interpreter::{value::NeoObject, Interpreter}};

impl Interpreter {
    pub fn interpret_statement_class(&mut self , class: &Class) -> NeoObject {
        todo!("not implemented")
    }
}